[profile.default]
src = "src"
out = "out"
libs = ["lib"]

solc = "0.8.28"

optimizer = true
optimizer_runs = 200

[dependencies]
account-abstraction = { version = "0.7.0", url = "https://github.com/eth-infinitism/account-abstraction.git" }