# Foundry (Smart Contracts)
# NOTE: lib/ is intentionally NOT ignored — dependencies are tracked as git submodules.
# NOTE: .gas-snapshot is intentionally tracked so `forge snapshot --check` is reproducible.
Contracts/out/
Contracts/cache/
Contracts/broadcast/

# Node.js (Frontend/Backend)
node_modules/
Contracts/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment Variables (SANGAT PENTING!)
# Ini nge-ignore semua file .env di root maupun subfolder
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
**/.env

# IDE & OS
.vscode/
.idea/
.DS_Store
Thumbs.db